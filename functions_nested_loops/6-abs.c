#include "main.h"
/**
 * _abs - prints the absolute value of a number
 *
 * @r: a number
 *
 * Return: returns the absolute value of a number
 */

int _abs(int r)
{
if (r < 0)
return( r * -1);
else
return(r);
}
