#include "main.h"
/**
 * _isupper - returns a value deppending if int is uppercase or not
 *
 * @c: a character
 *
 * Return: 1 if uppercase, 0 if lowercase
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
{
	return (1);
}
	else
{
	return (0);
}
}

